package com.parser;

import java.util.Comparator;

public class LockedInventoryComparator implements Comparator<LockedInventoryItemDto> {
    @Override
    public int compare(LockedInventoryItemDto o1, LockedInventoryItemDto o2) {
        return 0;
    }
}
